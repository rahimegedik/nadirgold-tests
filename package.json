{
  "name": "nadirgold-tests",
  "version": "1.0.0",
  "description": "NadirGold QA Automation Suite",
  "scripts": {
    "auth":   "playwright test tests/auth.spec.ts --project=auth-setup --headed",
    "test":   "playwright test --project=chromium",
    "test:headed": "playwright test --project=chromium --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0"
  }
}
